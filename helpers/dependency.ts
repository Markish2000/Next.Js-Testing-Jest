// Define una función llamada fetchData que simula realizar una solicitud de datos a algún servicio
export function fetchData() {
  // Simula la obtención de datos de algún servicio
  const data = 'Datos obtenidos.';

  // Retorna una cadena que representa los datos obtenidos
  return data;
}
